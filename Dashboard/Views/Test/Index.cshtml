@model IEnumerable<Dashboard.Models.FormSubmission>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Form.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Form.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FormSubmissionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FormSubmissionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FormSubmissionID })
        </td>
    </tr>
}

</table>
